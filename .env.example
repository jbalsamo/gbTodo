# Copy to .env.local and fill in values from the Supabase dashboard
# (Project Settings → API). Never commit real keys.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Legacy alias still accepted by the app if PUBLISHABLE_KEY is unset:
# VITE_SUPABASE_ANON_KEY=
